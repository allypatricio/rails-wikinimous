<h2><%= @article.title %></h2>
<p><%= @article.content %></p>
 <%= link_to article_path(@article), method: :delete, class: 'text-danger', data: { confirm: 'Are you sure?' } do %>
<i class="far fa-trash-alt"></i>
<% end %>
<%= link_to 'Edit article', edit_article_path(@article) %>
<i class="fas fa-pencil-alt"></i>

